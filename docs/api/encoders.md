# Feature Encoding

This section documents encoding utilities provided by **eb-features**
for transforming non-numeric features into numeric representations.

These encoders are intentionally stateless and operate only on the
provided dataset.

All content below is generated automatically from NumPy-style docstrings
in the source code.

## Encoding API

::: eb_features.panel.encoders
